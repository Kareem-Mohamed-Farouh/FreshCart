import { ChangerheartDirective } from './changerheart.directive';

describe('ChangerheartDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangerheartDirective();
    expect(directive).toBeTruthy();
  });
});
